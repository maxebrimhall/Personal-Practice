function deepSum(value){
    let sum = 0
    if(typeof value === 'number'){
        return sum += value
    }
//Checks for Arrays
    if(Array.isArray(value)){
        return value.reduce((s, value) => s + deepSum(value)) 
    }
}
deepSum(5)
