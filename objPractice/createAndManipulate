/*Goal: Create an object person with properties firstName, lastName, age, and hobbies (array).
Requirements: Build the object, write a function getFullName(person) that returns "First Last", and addHobby(person, hobby) that pushes a hobby.
Example: Input: person = {firstName: 'Ada', lastName:'Lovelace', age: 28, hobbies: ['math']}; getFullName(person) → 'Ada Lovelace'; after addHobby(person, 'programming') -> person.hobbies includes 'programming'.
Hint: Use dot notation and Array.prototype.push.
Extension: Add validation in addHobby to avoid duplicate hobbies.
Challenge 2 — Nested Objects and Property Access*/

let person = {
    firstName: "Randy",
    lastName: "Luvuere",
    age: 17,
    hobbies: ["Video Games", "Reading", "Scripting"]
}
let addHobby = (person, hobby) => {
    if(person.hobbies.includes(hobby)){
        return person.hobbies
    }
    person.hobbies.push(hobby)
}
let removeHobby = (person, hobby) => {
   console.log(person)
   return person
}
addHobby(person, "Beekeeping")